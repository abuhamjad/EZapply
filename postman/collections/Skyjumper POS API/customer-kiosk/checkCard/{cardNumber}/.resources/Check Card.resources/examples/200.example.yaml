$kind: http-example
request:
  url: "{{baseUrl}}/customer-kiosk/checkCard/:cardNumber"
  method: GET
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  pathVariables:
    cardNumber: ""
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000

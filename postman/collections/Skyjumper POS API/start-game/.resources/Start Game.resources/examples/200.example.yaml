$kind: http-example
request:
  url: "{{baseUrl}}/start-game"
  method: POST
  headers:
    api-x-key: "{{api_x_key}}"
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "card_no": "1000000001",
        "machine_id": "M001"
      }
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000

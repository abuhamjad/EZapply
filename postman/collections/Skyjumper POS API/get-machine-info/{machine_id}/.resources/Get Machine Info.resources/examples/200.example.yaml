$kind: http-example
request:
  url: "{{baseUrl}}/get-machine-info/:machine_id"
  method: GET
  headers:
    api-x-key: "{{api_x_key}}"
  pathVariables:
    machine_id: ""
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000

$kind: http-example
request:
  url: "{{baseUrl}}/manage-game-session"
  method: POST
  headers:
    api-x-key: "{{api_x_key}}"
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "face_id": "FACE123"
      }
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000

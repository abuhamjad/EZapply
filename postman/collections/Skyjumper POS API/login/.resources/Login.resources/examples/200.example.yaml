$kind: http-example
request:
  url: "{{baseUrl}}/login"
  method: POST
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "email": "employee@example.com",
        "password": "Password123"
      }
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000

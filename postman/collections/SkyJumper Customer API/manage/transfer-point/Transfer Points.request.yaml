$kind: http-request
description: Auth required. Body is forwarded as-is to Sp_TransferCustomerPlayPoint; adjust field names to match the stored procedure's expected schema.
url: "{{baseUrl}}/manage/transfer-point"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "from_card_id": "1",
      "points": 100,
      "to_mobile": "9888888888"
    }
settings: {}
examples: ./.resources/Transfer Points.resources/examples
order: 1000

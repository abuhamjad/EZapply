$kind: http-example
request:
  url: "{{baseUrl}}/manage/get-card-transactions?limit=10&offset=0"
  method: GET
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    limit: "10"
    offset: "0"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000

$kind: http-request
description: Auth required. limit/offset are optional (default 0).
url: "{{baseUrl}}/manage/get-card-transactions?limit=10&offset=0"
method: GET
queryParams:
  limit: "10"
  offset: "0"
settings: {}
examples: ./.resources/Get Card Transactions.resources/examples
order: 1000

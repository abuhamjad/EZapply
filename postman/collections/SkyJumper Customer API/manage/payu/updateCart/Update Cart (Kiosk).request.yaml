$kind: http-request
description: Auth required. `type` is either 'update' or 'delete'.
url: "{{baseUrl}}/manage/payu/updateCart"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "cart_id": 1,
      "qty": 3,
      "type": "update"
    }
settings: {}
examples: ./.resources/Update Cart (Kiosk).resources/examples
order: 1000
